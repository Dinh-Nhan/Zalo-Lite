name: frontend
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  go_router: ^17.1.0
  cupertino_icons: ^1.0.8
  firebase_core: ^2.24.0
  cloud_firestore: ^4.17.5

  provider: ^6.1.1
  camera: ^0.10.5+5
  permission_handler: ^11.0.1
  flutter_ringtone_player: ^3.2.0 # Để tạo tiếng chuông
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
